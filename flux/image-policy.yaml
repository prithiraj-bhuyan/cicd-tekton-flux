apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: helloimagepol
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: hello-app
  filterTags:
    pattern: '^(?P<ts>.*)-.*'
    extract: '$ts'
  policy:
    numerical:
      order: asc
